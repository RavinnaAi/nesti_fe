'use client';

import PublicAboutSection from '../PublicAboutSection';

export default function BrokerAboutSection({ about, profile }) {
  return <PublicAboutSection about={about} profile={profile} role="mortgage_broker" />;
}

