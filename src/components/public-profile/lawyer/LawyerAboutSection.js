'use client';

import PublicAboutSection from '../PublicAboutSection';

export default function LawyerAboutSection({ about, profile }) {
  return <PublicAboutSection about={about} profile={profile} role="lawyer" />;
}

