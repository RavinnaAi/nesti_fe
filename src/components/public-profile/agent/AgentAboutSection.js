'use client';

import PublicAboutSection from '../PublicAboutSection';

export default function AgentAboutSection({ about, profile }) {
  return <PublicAboutSection about={about} profile={profile} role="agent" />;
}

